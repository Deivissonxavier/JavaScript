// Configuração
let sum = 0;

function addThree() {
  sum = sum + 3;
}

// Altere apenas o código abaixo desta linha
function addFive(){
  sum = sum + 5;
}

// Altere apenas o código acima desta linha

addThree();
addFive();